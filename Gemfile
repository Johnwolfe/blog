source 'http://rubygems.org'
ruby '2.1.0'


gem 'sinatra'   # our web app library
gem 'activerecord'  # the ORM that will talk to DB
gem 'sinatra-activerecord'   #lib that helps sinatra use activerecord
#gem 'sqlite3'             #the 'adapter'
gem 'rake'
gem 'rack-flash3'

group :development do
 	gem 'sqlite3'
end

group :production do
 	gem 'pg', "~> 0.17.1"
	end
